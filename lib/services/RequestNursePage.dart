import 'package:flutter/material.dart';

class RequestNursePage extends StatelessWidget {
  const RequestNursePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('Request Nurse Page'),
    );
  }
}
